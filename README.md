# My Custom Bootstrap CSS

This repository is a **practice project** for customizing Bootstrap using Sass and exploring a specific design style.  
It is used to experiment with Bootstrap variable overrides, typography, and component styling.

## Usage

### Install dependencies:
```bash
npm install
```

### Compile Sass:
```bash
npm run watch
```